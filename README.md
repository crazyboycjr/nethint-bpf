# nethint-bpf

```
git submodule update --init --recursive
cargo r
sudo -E cargo r <interface>
```

using nix flakes
```
git submodule update --init --recursive
nix develop
cargo r
sudo -E cargo r <interface>
```
